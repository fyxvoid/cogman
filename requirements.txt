# ── ALWAYS-ON (Tier 1 & 2, zero API needed) ─────────────────────────────────
psutil>=5.9.0           # system monitoring
rapidfuzz>=3.0.0        # fuzzy NLP matching

# ── Pi Agent Core: LLM providers (install at least ONE) ──────────────────────
# Anthropic Claude (best overall)
anthropic>=0.49.0

# OpenAI GPT-4o (also enables Groq via openai package)
# openai>=1.30.0

# Google Gemini
# google-generativeai>=0.8.0

# ── Hermes Skills/Plugins: YAML manifests ─────────────────────────────────────
pyyaml>=6.0.0

# ── OpenClaw Gateway: messaging platforms ────────────────────────────────────
# Telegram bot
# python-telegram-bot>=21.0

# Discord bot
# discord.py>=2.3.0

# Slack bot
# slack-bolt>=1.18.0

# ── REST API / Webhook gateway ────────────────────────────────────────────────
# fastapi>=0.110.0
# uvicorn>=0.27.0

# ── Rich CLI (optional but strongly recommended) ─────────────────────────────
rich>=13.0.0

# ── Web / Browser tools ───────────────────────────────────────────────────────
# Better HTML extraction (much better than regex)
# trafilatura>=1.6.0

# Beautiful Soup for HTML parsing
# beautifulsoup4>=4.12.0
# lxml>=5.0.0

# ── Code tools ────────────────────────────────────────────────────────────────
# Python formatter (optional)
# black>=24.0.0

# Fast linter
# ruff>=0.4.0

# ── Image tools ───────────────────────────────────────────────────────────────
# Pillow for image resize/convert
# Pillow>=10.0.0

# PDF reading
# pypdf>=4.0.0

# ── Speech: TTS offline ────────────────────────────────────────────────────────
pyttsx3>=2.90           # RECOMMENDED: offline TTS

# ── Speech: STT offline ────────────────────────────────────────────────────────
vosk>=0.3.45            # offline STT (~50MB model)
sounddevice>=0.4.6      # mic input

# ── Memory: vector search (improves semantic recall) ─────────────────────────
# chromadb>=0.4.0

# ── Local LLM via Ollama ──────────────────────────────────────────────────────
# Install Ollama: curl -fsSL https://ollama.ai/install.sh | sh
# Pull a model:   ollama pull mistral
# cogman auto-detects Ollama at localhost:11434

# ── Search ────────────────────────────────────────────────────────────────────
# Brave Search API (optional, DuckDuckGo works without key)
# Set BRAVE_API_KEY to enable

# ── Image generation ──────────────────────────────────────────────────────────
# Set OPENAI_API_KEY for DALL-E 3
# Set COGMAN_SD_URL for local Stable Diffusion (AUTOMATIC1111)

# ── Quick install for full power ─────────────────────────────────────────────
# pip install anthropic openai pyyaml rich trafilatura beautifulsoup4 Pillow ruff
